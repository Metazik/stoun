# Copy to .env.local and fill in to run against a real Supabase project.
# Leave unset to run in demo mode (in-memory data, no login required).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
